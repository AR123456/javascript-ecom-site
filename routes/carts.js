// create the router object and associate rotes to it

const express = require("express");

const router = express.Router();
// receive  add item post request to add item to a cart

// receive GET request to show all items in cart

// receive POST request to delete an item from a cart

module.exports = router;
// will need to require this in the root index.js
// ///routes needed
// will need add with form submit to go into a shopping chart
// will need to shopping cart page with list of products in it
// will need a route for the delete button inside the cart page
